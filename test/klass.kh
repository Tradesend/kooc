@namespace(NAMESPACE) {
    @class CLASS () {
        @destructor()                           @callable(call: public);
        @constructor(int a, char* av)           @callable(call: public);

        int     test_attribute                  @property(get: public, set: public);

        int            method(int)              @callable(call: public, virtual);
    };
};

int main() {
    @call(NAMESPACE@CLASS@methode)(t);
}